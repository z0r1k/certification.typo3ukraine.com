options {

  # Add save-and-new to all forms
  saveDocNew = 1
  
  # Enable it eg. for content-elements only
  # saveDocNew = 0
  # saveDocNew.tt_content = 1
  
  # Hide the delete button
  # disableDelete = 1

  # And again for hiding in content-element only
  # disableDelete = 0
  # disableDelete.tt_content = 1
  
  # Clear cache for normal user
  # clearCache.pages = 1
  # clearCache.all = 1
}